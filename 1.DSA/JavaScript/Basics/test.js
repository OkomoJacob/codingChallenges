// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');

function findStrictlyInsideCircle(inner, outer, points_x, points_y) {
    /* write your code in JavaScript (Node.js 8.9.4) */
    /* 1. Define the equation of a circle
       2. Set an empty array and precompute the location of each of the given points(x^2 + y^2) of each point coordinates 
       3. Store the individual circle values
       4. Query for each points and check if the condition is true
       5. Perform a Binary Search
       6. Return only when True
    */
}